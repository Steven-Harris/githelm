/**
 * Firebase Integration Main Module
 * Exports Firebase functionality from a single entry point
 */

// Export types
export * from './types';

// Export Firebase client instance
export { firebase, authState } from './client';